# ===========================================
# Violence Detection System - Dependencies
# ===========================================
# Install with: pip install -r requirements.txt
# Requires Python 3.11 (3.13 has no wheels for this numpy/TF combo).
# ===========================================

# Core Deep Learning
#
# Version pins (DO NOT loosen without re-checking the protobuf chain):
#   TensorFlow 2.19+ ships protobuf 6.x-generated code and enforces a
#   strict gencode/runtime version check; MediaPipe needs protobuf 4.x.
#   TF 2.17.x predates that check entirely (protobuf 4.25 gencode), so
#   TF 2.17.1 + protobuf 4.25.x + MediaPipe 0.10.14 all coexist.
#   Pinned EXACTLY (==) not as a range — pip's resolver kept a newer TF
#   when given a range against an already-messy environment.
#   MediaPipe 0.10.14 is the last release with the legacy mp.solutions
#   API this project's core/pose_extractor.py depends on.
tensorflow==2.17.1
keras>=3.0.0
protobuf>=4.25.3,<5

# Computer Vision
opencv-python>=4.5.5.64
mediapipe==0.10.14

# YOLO Object Detection (Multi-person detection)
ultralytics>=8.0.0

# Scientific Computing
numpy>=1.23.0,<2.0.0
scipy>=1.10.0
pandas>=1.5.0

# Machine Learning Utilities
scikit-learn>=1.2.0

# Model Storage
h5py>=3.8.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Web Dashboard
flask>=2.0.0
flask-cors>=3.0.10
flask-socketio>=5.3.0
flask-jwt-extended>=4.5.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0  # Only needed when using PostgreSQL (DATABASE_URL env var)

# Utilities
tqdm>=4.64.0
PyYAML>=6.0

# Optional: Audio alerts on Linux (uncomment if needed)
# pygame>=2.1.0

# Development (optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0
